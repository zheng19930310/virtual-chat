# 🚀 快速启动 - 3步开始使用

## 当前状态

⚠️ **检测到您的系统未安装 Java 和 Maven**

## 📋 启动步骤

### 第1步：安装环境（二选一）

#### 选项A：自动安装（推荐，需要Chocolatey）

双击运行：
```
install_env.bat
```

如果没有 Chocolatey，先安装它：
```powershell
# 以管理员身份运行 PowerShell
Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
```

#### 选项B：手动安装

查看详细说明：[STARTUP_GUIDE.md](file:///d:/aiwork/virtual-chat/STARTUP_GUIDE.md)

简要步骤：
1. 下载并安装 Java JDK 17：https://adoptium.net/temurin/releases/?version=17
2. 下载并安装 Maven：https://maven.apache.org/download.cgi
3. 配置环境变量

### 第2步：验证环境

打开新的命令行窗口，运行：
```powershell
java -version
mvn -version
```

如果都显示版本信息，说明安装成功！✅

### 第3步：启动项目

双击运行：
```
start.bat
```

或在命令行运行：
```powershell
cd d:\aiwork\virtual-chat
mvn spring-boot:run
```

---

## 🌐 访问应用

启动成功后，浏览器打开：
```
http://localhost:8080
```

登录信息：
- 用户名：`hibaobao`
- 密码：`hibaobao`

---

## ⚙️ （可选）配置 API 密钥

要启用完整的 AI 功能，需要配置 API 密钥：

1. 访问：https://dashscope.console.aliyun.com/
2. 注册/登录，创建 API 密钥
3. 编辑文件：`src\main\resources\application.properties`
4. 替换这一行：
   ```properties
   spring.ai.alibaba.qwen.api-key=sk-你的真实密钥
   ```

**注意：** 不配置密钥也可以启动，但 AI 回复会使用模拟数据。

---

## 📚 更多帮助

- 📖 详细启动指南：[STARTUP_GUIDE.md](file:///d:/aiwork/virtual-chat/STARTUP_GUIDE.md)
- 🔧 实现指南：[IMPLEMENTATION_GUIDE.md](file:///d:/aiwork/virtual-chat/IMPLEMENTATION_GUIDE.md)
- 📋 功能清单：[CHECKLIST.md](file:///d:/aiwork/virtual-chat/CHECKLIST.md)
- 🎯 快速参考：[QUICK_REFERENCE.md](file:///d:/aiwork/virtual-chat/QUICK_REFERENCE.md)

---

## ❓ 常见问题

**Q: 双击 start.bat 后提示"未检测到Java环境"？**  
A: 请先运行 `install_env.bat` 或手动安装 Java 和 Maven

**Q: 安装完成后还是提示找不到 Java？**  
A: 请重启命令行窗口或重新登录系统，让环境变量生效

**Q: 启动很慢？**  
A: 首次启动需要下载依赖，可能需要几分钟，请耐心等待

**Q: 端口8080被占用？**  
A: 编辑 `application.properties`，修改 `server.port=8081`

---

**准备好了吗？从第1步开始吧！** 🎉