# 虚拟社交聊天Web工具项目需求文档

## 项目概述
本项目是一个基于Java Spring Alibaba AI和千问大模型的虚拟社交聊天Web工具，用户可以创建虚拟朋友并与之进行智能对话。系统提供类似微信的聊天界面，支持好友管理和个性化设置。

## 技术栈
- **后端框架**: Java Spring Boot + Spring Alibaba AI
- **AI模型**: 通义千问大模型 (Qwen)
- **数据库**: H2内存数据库 (轻量级小型数据库)
- **前端**: HTML/CSS/JavaScript + Bootstrap
- **构建工具**: Maven

## 功能需求

### 1. 用户认证模块
- 简单的登录验证机制
- 固定账号: `hibaobao`
- 固定密码: `hibaobao`
- 登录状态保持 (Session管理)

### 2. 好友管理功能
#### 添加好友
- 为虚拟朋友设置以下属性:
  - 姓名 (必填)
  - 年龄 (数字)
  - 性别 (男/女/其他)
  - 性格特征 (文本描述)
  - 主要人物特点描述 (详细文本)
  
#### 修改好友信息
- 支持后期修改所有好友属性
- 实时保存更新

#### 删除好友
- 支持删除不需要的虚拟朋友

#### 好友列表展示
- 显示所有已添加的虚拟朋友
- 点击好友进入聊天界面

### 3. 聊天功能
#### 聊天界面设计
- 类似微信的聊天界面布局
- 左侧好友列表，右侧聊天区域
- 消息气泡样式区分发送方和接收方
- 右上角显示"连续对话"开关按钮

#### 智能对话
- 集成通义千问大模型实现智能回复
- 根据好友的性格特点和人物描述生成符合角色的回复
- 支持两种对话模式：
  - **单条交流模式**（默认）：每次对话独立，不携带上下文
  - **连续对话模式**：携带历史对话上下文，实现连贯对话
- 用户可通过右上角按钮切换对话模式
- 支持思维链模式选择：
  - **常规模式**（默认）：保持原有聊天效果，不添加额外提示
  - **COT (Chain of Thought)**：逐步思考并给出答案
  - **TOT (Tree of Thoughts)**：探索多种可能的解决方案并选择最佳答案
- 用户可通过右上角单选框选择思维链模式
- **流式输出功能**（仅 COT/TOT 模式）：
  - 思维链处理过程在可伸缩的文本域中实时展示
  - 思维过程具有层次结构，便于阅读理解
  - 思维推导完成后自动折叠起来
  - 最终回答结果隔行显示在思维链下方
  - 使用 Server-Sent Events (SSE) 实现流式输出

#### 消息管理
- 实时显示聊天记录
- 支持滚动查看历史消息
- 消息时间戳显示
- 支持文本消息和图片消息

#### 场景图片生成功能
- 当用户发送包含"帮我生成'XX'一段场景的描述"格式的消息时
- 系统自动识别并调用图像生成API
- 基于对话内容和场景描述生成4张相关图片
- 图片以消息形式展示在聊天界面中
- 支持图片预览和下载

### 4. 数据隔离
- 每个虚拟朋友的数据完全隔离
- 聊天记录仅对对应的好友可见
- 不同好友之间的对话上下文互不影响
- 确保用户隐私和数据安全

### 5. 数据持久化
- 使用H2数据库存储用户信息、好友数据和聊天记录
- 自动建表和初始化数据
- 支持对话上下文的历史记录存储

## 系统架构

### 后端架构
```
com.virtualchat
├── controller      # 控制器层
├── service         # 业务逻辑层
├── repository      # 数据访问层
├── model           # 实体类
├── config          # 配置类
└── dto             # 数据传输对象
```

### 前端架构
- 单页面应用结构
- RESTful API交互
- 响应式设计适配不同设备

## 数据库设计

### 用户表 (users)
- id: 主键
- username: 用户名
- password: 密码

### 好友表 (friends)
- id: 主键
- user_id: 关联用户ID
- name: 朋友姓名
- age: 年龄
- gender: 性别
- personality: 性格特征
- characteristics: 主要人物特点描述
- created_at: 创建时间
- updated_at: 更新时间

### 聊天消息表 (messages)
- id: 主键
- friend_id: 关联好友ID
- sender: 发送方 (user/friend)
- content: 消息内容
- message_type: 消息类型 (text/image)
- image_urls: 图片URL列表 (JSON格式，仅图片消息使用)
- timestamp: 发送时间
- context_id: 上下文ID (用于连续对话分组)

### 对话上下文表 (conversations)
- id: 主键
- friend_id: 关联好友ID
- context_data: 上下文数据 (JSON格式，存储最近N轮对话)
- is_active: 是否激活连续对话模式
- created_at: 创建时间
- updated_at: 更新时间

## API接口设计

### 认证相关
- POST `/api/login` - 用户登录
- POST `/api/logout` - 用户登出
- GET `/api/auth/check` - 检查登录状态

### 好友管理
- GET `/api/friends` - 获取好友列表
- POST `/api/friends` - 添加好友
- PUT `/api/friends/{id}` - 修改好友信息
- DELETE `/api/friends/{id}` - 删除好友

### 聊天功能
- GET `/api/messages/{friendId}` - 获取与指定好友的聊天记录
- POST `/api/messages` - 发送消息并获取AI回复
- POST `/api/messages/context` - 切换连续对话模式
- GET `/api/messages/context/{friendId}` - 获取当前对话上下文状态
- POST `/api/images/generate` - 生成场景图片

## 安全考虑
- 基础的身份验证
- Session管理防止未授权访问
- 输入验证防止注入攻击

## 性能优化
- 合理的数据库索引设计
- 缓存常用数据减少数据库查询
- 异步处理AI请求提升响应速度

## 部署说明
- 打包为可执行JAR文件
- 内置H2数据库无需额外配置
- 支持直接运行启动服务