# 🚨 启动前必读 - 环境安装指南

## ⚠️ 当前问题

您的系统**未安装** Java 和 Maven，无法直接启动项目。

---

## ✅ 解决方案（3选1）

### 方案1：使用PowerShell脚本自动安装（最简单）

**步骤：**

1. **右键点击** `install_env.ps1` 文件
2. 选择 **"以管理员身份运行"**
3. 等待自动安装完成（约5-10分钟）
4. 关闭窗口，重新打开新的命令行
5. 运行 `start.bat` 启动项目

---

### 方案2：手动下载安装（推荐新手）

#### 第1步：安装 Java JDK 17

1. **下载页面：** https://adoptium.net/temurin/releases/?version=17
   
2. **选择：** Windows x64 MSI 安装包

3. **安装：**
   - 双击下载的 `.msi` 文件
   - 一路点击"下一步"
   - 记住安装路径（默认：`C:\Program Files\Eclipse Adoptium\jdk-17.x.x.x-hotspot`）

4. **配置环境变量：**
   - 右键"此电脑" → "属性" → "高级系统设置"
   - 点击"环境变量"
   - 在"系统变量"中找到 `Path`，点击"编辑"
   - 点击"新建"，添加：`C:\Program Files\Eclipse Adoptium\jdk-17.x.x.x-hotspot\bin`
   - （将路径替换为你的实际安装路径）
   - 点击"确定"保存

5. **验证：**
   - 打开**新的** CMD 或 PowerShell 窗口
   - 输入：`java -version`
   - 应该显示版本信息

#### 第2步：安装 Maven

1. **下载页面：** https://maven.apache.org/download.cgi

2. **下载：** `apache-maven-3.9.x-bin.zip`

3. **解压：**
   - 解压到：`C:\Program Files\Apache\maven`
   - （如果没有Apache文件夹，先创建）

4. **配置环境变量：**
   - 打开"环境变量"
   - 点击"新建"系统变量：
     - 变量名：`MAVEN_HOME`
     - 变量值：`C:\Program Files\Apache\maven`
   - 编辑 `Path` 变量，添加：`%MAVEN_HOME%\bin`
   - 点击"确定"保存

5. **验证：**
   - 打开**新的** CMD 或 PowerShell 窗口
   - 输入：`mvn -version`
   - 应该显示Maven版本信息

#### 第3步：启动项目

```powershell
cd d:\aiwork\virtual-chat
start.bat
```

---

### 方案3：使用IDE（适合开发者）

如果您有 IntelliJ IDEA：

1. **打开项目：**
   - File → Open → 选择 `d:\aiwork\virtual-chat` 文件夹

2. **等待依赖下载：**
   - IDEA会自动检测Maven项目
   - 右下角会显示依赖下载进度

3. **运行项目：**
   - 找到 `src/main/java/com/virtualchat/VirtualChatApplication.java`
   - 右键 → Run 'VirtualChatApplication'

---

## 📝 安装检查清单

完成以下检查后再启动：

- [ ] 已安装 Java JDK 17
- [ ] 已配置 JAVA_HOME 或 Path 环境变量
- [ ] 运行 `java -version` 显示版本信息
- [ ] 已安装 Maven 3.6+
- [ ] 已配置 MAVEN_HOME 和 Path 环境变量
- [ ] 运行 `mvn -version` 显示版本信息
- [ ] **重要：** 已关闭所有旧的命令行窗口，打开了新窗口

---

## 🎯 快速验证

打开**新的** PowerShell 或 CMD，依次运行：

```powershell
# 检查Java
java -version

# 检查Maven
mvn -version
```

如果两个命令都显示版本信息（而不是"不是内部或外部命令"），说明安装成功！✅

---

## 🚀 启动项目

环境准备好后：

```powershell
cd d:\aiwork\virtual-chat
start.bat
```

首次启动会下载依赖，可能需要3-5分钟，请耐心等待。

看到以下信息表示启动成功：
```
Started VirtualChatApplication in X.XXX seconds
```

---

## 🌐 访问应用

浏览器打开：
```
http://localhost:8080
```

登录：
- 用户名：`hibaobao`
- 密码：`hibaobao`

---

## ❓ 常见问题

### Q1: 安装后运行 `java -version` 还是提示找不到？

**A:** 
1. 确保已关闭所有旧的命令行窗口
2. 打开**新的**窗口再试
3. 如果还不行，重启电脑

### Q2: Maven下载依赖很慢？

**A:** 
- 首次启动需要下载大量依赖，这是正常的
- 可以使用国内镜像加速（可选）
- 耐心等待，不要中断

### Q3: 端口8080被占用？

**A:** 
编辑 `src\main\resources\application.properties`，修改：
```properties
server.port=8081
```

### Q4: 启动报错 "Unsupported class file major version"？

**A:** 
Java版本不对，确保安装的是 Java 17 或更高版本。

---

## 📞 需要帮助？

如果遇到问题：

1. 查看完整的启动指南：[STARTUP_GUIDE.md](file:///d:/aiwork/virtual-chat/STARTUP_GUIDE.md)
2. 检查环境是否正确安装
3. 确认已打开新的命令行窗口
4. 查看控制台错误信息

---

**准备好了吗？从方案1开始吧！** 🎉

推荐使用 **方案1（PowerShell脚本）**，最简单快捷！