# 🚀 超简单安装指南（5分钟完成）

## ⚠️ 重要提示

由于权限限制，自动安装脚本无法运行。请按照以下步骤**手动安装**，非常简单！

---

## 📥 第1步：下载并安装 Java（2分钟）

### 方法A：使用安装包（推荐新手）

1. **点击下载：** [Eclipse Temurin JDK 17](https://adoptium.net/temurin/releases/?version=17)

2. **选择：** Windows x64 MSI 安装包
   - 文件名类似：`OpenJDK17U-jdk_x64_windows_hotspot_17.x.x.msi`

3. **安装：**
   - 双击下载的 `.msi` 文件
   - 一路点击"下一步"或"Next"
   - 等待安装完成

4. **验证：**
   - 按 `Win + R`，输入 `cmd`，回车
   - 输入：`java -version`
   - 应该看到版本信息

### 方法B：使用压缩包（免安装）

1. 下载 ZIP 版本
2. 解压到：`C:\Java\jdk-17`
3. 配置环境变量（见下方）

---

## 📥 第2步：下载并安装 Maven（2分钟）

1. **点击下载：** [Apache Maven](https://maven.apache.org/download.cgi)
   - 选择：`apache-maven-3.9.x-bin.zip`

2. **解压：**
   - 解压到：`C:\Maven\apache-maven-3.9.x`

3. **配置环境变量：**
   
   **步骤：**
   1. 右键"此电脑" → "属性"
   2. 点击"高级系统设置"
   3. 点击"环境变量"
   4. 在"系统变量"区域，点击"新建"
      - 变量名：`MAVEN_HOME`
      - 变量值：`C:\Maven\apache-maven-3.9.x`（你的实际路径）
   5. 找到 `Path` 变量，点击"编辑"
   6. 点击"新建"，添加：`%MAVEN_HOME%\bin`
   7. 点击"确定"保存所有窗口

---

## ✅ 第3步：验证安装

1. **关闭所有**命令行窗口
2. **打开新的** CMD 或 PowerShell
3. 依次运行：

```cmd
java -version
mvn -version
```

如果两个命令都显示版本信息，说明成功！✅

**成功的标志：**
```
java version "17.0.x" ...
Apache Maven 3.9.x ...
```

---

## 🚀 第4步：启动项目

环境准备好后，在项目目录运行：

```cmd
cd d:\aiwork\virtual-chat
start.bat
```

或者直接**双击** `start.bat` 文件

**首次启动：**
- 会下载依赖，需要3-5分钟
- 请耐心等待
- 看到 `Started VirtualChatApplication` 表示成功

---

## 🌐 第5步：访问应用

浏览器打开：
```
http://localhost:8080
```

登录：
- 用户名：`hibaobao`
- 密码：`hibaobao`

---

## 🎯 快速检查清单

在安装前，请确认：

- [ ] 已下载 Java JDK 17 安装包
- [ ] 已安装 Java
- [ ] 已下载 Maven 压缩包
- [ ] 已解压 Maven
- [ ] 已配置 MAVEN_HOME 环境变量
- [ ] 已在 Path 中添加 `%MAVEN_HOME%\bin`
- [ ] 已关闭旧的命令行窗口
- [ ] 已打开新的命令行窗口验证

---

## ❓ 常见问题

### Q1: 安装后运行 `java -version` 还是提示找不到？

**解决：**
1. 确保已**关闭所有旧窗口**
2. 打开**新的**CMD窗口
3. 如果还不行，**重启电脑**

### Q2: 不知道如何配置环境变量？

**详细步骤：**
1. 按 `Win + S`，搜索"环境变量"
2. 点击"编辑系统环境变量"
3. 点击"环境变量"按钮
4. 在"系统变量"区域操作
5. 配置完成后，一定要点击"确定"保存

### Q3: Maven下载很慢？

**解决：**
- 首次启动需要下载大量依赖，这是正常的
- 可以使用国内镜像（可选）
- 耐心等待，不要中断

### Q4: 想跳过手动配置？

**替代方案：**
使用 IntelliJ IDEA：
1. 下载安装 IDEA：https://www.jetbrains.com/idea/
2. 打开项目文件夹
3. IDEA会自动处理依赖
4. 直接运行主类即可

---

## 📞 需要帮助？

如果遇到问题：

1. 截图错误信息
2. 查看：[STARTUP_GUIDE.md](file:///d:/aiwork/virtual-chat/STARTUP_GUIDE.md)
3. 或查看详细的环境配置教程

---

## 💡 小贴士

- Java和Maven只需要安装一次
- 以后可以直接运行 `start.bat` 启动项目
- API密钥已配置，可以直接使用AI功能

---

**开始安装吧！整个过程只需5分钟！** 🎉